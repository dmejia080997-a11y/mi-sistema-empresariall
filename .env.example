PORT=3000
SESSION_SECRET=change_me
OPENAI_API_KEY=
DATABASE_PATH=./data/app.db
# Use PostgreSQL when DATABASE_URL is set. Leave empty to use SQLite.
DATABASE_URL=
DATABASE_SSL=false
# Required for automatic per-company PostgreSQL databases:
# DATABASE_URL user must be able to CREATE DATABASE.
